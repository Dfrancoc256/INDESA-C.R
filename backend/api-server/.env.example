DATABASE_URL=postgresql://indesa_user:your_password@127.0.0.1:5432/indesa
JWT_SECRET=change-this-secret-before-deploying
PORT=4000
FRONTEND_PORT=5173

# Optional. Only use this when the frontend and backend are hosted on different origins.
# Leave it empty in local development so the Vite proxy can handle /api requests.
VITE_BACKEND_PUBLIC_URL=
